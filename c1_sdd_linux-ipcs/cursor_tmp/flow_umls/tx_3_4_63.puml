@startuml
!pragma layout smetana
skinparam conditionStyle insideDiamond
skinparam linetype ortho
start
:err = IPC_SHM_E_OK;
if (id[instance].rxIrqNum == IPC_IRQ_NONE?) then (yes)
  if (id[instance].rxCallback != NULL?) then (yes)
    :err = rxCallback(instance, IPC_SOFTIRQ_BUDGET);
  else (no)
  endif
else (no)
  :err = -IPC_SHM_E_INVAL;
endif
:return err;
stop
@enduml

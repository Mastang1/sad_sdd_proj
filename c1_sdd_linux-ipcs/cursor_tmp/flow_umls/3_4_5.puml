@startuml
!pragma layout smetana
skinparam conditionStyle insideDiamond
skinparam linetype ortho
title ipcsQueueMemSize
start
:return (uint32)sizeof(struct IPCS_RING_TYPE) + (uint32)queue->elem_num * (uint32)queue->elem_size;
stop
@enduml

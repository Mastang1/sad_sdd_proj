# Format final_sdd.docx file
---
## 要求：
 - 1.所有修改，必须使用python-docx库，开发对应的python脚本来实现；脚本同一存储在工作区根路径下的format_docx_py文件夹中；
 - 2.每个生成的工具脚本，需要添加完整、规范的中文注释，注释包括method注释及文档header注释；注释中要有详细的使用方式
 - 3.只能修改一下任务标题下指定的修改，不可修改未指定的任何内容；

---
## 约定： 符号TF即目标文件，指的是 C:\tangyapeng\docs\StarGather\c1_sdd\final_sdd.docx文件，后续任务重用TF代替它
## 任务：
 - 1. TF的各级标题中，有重复的数字文档标题号，这是由于自动拷贝时候遗留的问题，需要逐个删除，只保留每个标题行最前面的带样式的标题；
 - 2. 遍历TF中所有的表格，为每个表格添加默认的实线边框，保证每个表格都做处理
 - 3. 修改所有正文文本格式为：微软雅黑、11号字体、不加粗，无首行缩进，单倍行距、段前后0磅值；保证只修改正文，不修改任何标题，执行完成后，需要执行标题完整性检测；
 - 4.修改所有表格中的文字为：微软雅黑，大小为10，不加粗；
 - 5. 遍历所有插图，逐个缩放，直到满足如下条件：1. 每个插图都可以正常显示，不超过四个页边距；2.在满足条件1的情况下，修改每个插图的宽度为14cm；不满足1，优先按照1的条件执行；3. 3.2 Files章节特殊处理，修改所有图宽度为12cm；4.必须遵守的规则：所有的缩放都保持原始宽高比例；
 - 6. 遍历所有插图及图表，修改它们在文档布局中的位置：行首无缩进，居中对齐；注意是图或表相对于文档对齐，不是单元格内容；
 - 7. 删除、移动指定行：1. 操作范围章节3.2，删除3.2.1-3.2.18中的所有插图下的形如“3.2.2 ipc-queue.c 头文件依赖（组件 UML）”的类似的单行，此外不修改其他任何内容；2. 操作范围章节3.3/3.4，所有插图下的包含 processing flow的行，例如“3.3.1 processing flow”；然后在每个插图前写入 “processing flow”，格式为正文文本格式：微软雅黑、11号字体、不加粗，无首行缩进，单倍行距、段前后0磅值；

 ## 完善 C:\tangyapeng\docs\StarGather\c1_sdd\ipcs_sdd.md
  - 1. 你是一个资深嵌入式专家和aspice专家，以C:\tangyapeng\docs\StarGather\c1_sdd\aspice_cn_4_4.md规范的架构设计和详细设计章节为依据，分析C:\tangyapeng\docs\StarGather\c1_sdd\ipcs-architecture.pdf架构设计规范和C:\tangyapeng\docs\StarGather\c1_sdd\ipcs_sdd.md详细设计规范，形成一个当前的架构设计和软件详细设计文档的分析报告，说明不足及修改方案，分析报告存储在aya_report.md中；要求修改方案中要说明在具体什么位置增加或者 删除哪些标题及内容；所有的描述要遵循第一性原理